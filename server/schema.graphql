type Query {
    user: User
    people(id:String): People
}

type User {
    id: ID
    name: String
}

type People {
    id: ID
    name: String
    age:String
}

type Pet {
    id: ID
    name: String
}
